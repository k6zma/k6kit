name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  quality:
    name: Quality Gates
    runs-on: ubuntu-latest
    env:
      MIN_COVERAGE: "70.0"
      BENCH_NO_FIELDS_JSON_MAX_NS: "850.0"
      BENCH_NO_FIELDS_TEXT_MAX_NS: "500.0"
      BENCH_WITH_FIELDS_JSON_MAX_NS: "2400.0"
      BENCH_WITH_FIELDS_TEXT_MAX_NS: "1700.0"
      BENCH_WITH_CONTEXT_JSON_MAX_NS: "2100.0"
      BENCH_WITH_CONTEXT_TEXT_MAX_NS: "1300.0"
      BENCH_WITH_OTEL_JSON_MAX_NS: "1700.0"
      BENCH_WITH_OTEL_TEXT_MAX_NS: "1100.0"
      BENCH_DEBUG_DISABLED_JSON_MAX_NS: "90.0"
      BENCH_DEBUG_DISABLED_TEXT_MAX_NS: "90.0"

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Verify formatting (gofmt)
        run: |
          unformatted="$(gofmt -l .)"
          if [ -n "$unformatted" ]; then
            printf 'These files need gofmt:\n%s\n' "$unformatted"
            exit 1
          fi

      - name: Lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: latest
          args: ./...

      - name: Vet
        run: go vet ./...

      - name: Test
        run: go test ./...

      - name: Race Test
        run: go test -race ./...

      - name: Coverage Threshold
        run: bash ./scripts/coverage-guard.sh

      - name: Benchmark Thresholds
        run: bash ./scripts/bench-guard.sh
